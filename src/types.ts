export interface IProduct {
  id: number,
  name: string,
  brand: string,
  category: string,
  price: number,
  stock: number,
  color: string,
  size: number,
  season: string,
  use: string,
  dopImg: number[],
  description: string,
}
